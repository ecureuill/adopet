summary: Pets
get:
  summary: Get all pets
  responses:
    200:
      description: A list of pets to be returned
      content:
        application/json:
          schema: 
            $ref: ../schemas/pets.response.schema.yml
    500:
      description: Unexpected Error
      content:
        application/json:
          schema: 
            $ref: ../schemas/error.schema.yml
